[project]
name = "comfyui-llm-bikeshed"
version = "0.1.0"
description = "ComfyUI custom nodes for local LLM text generation"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.28.0",
]

[tool.comfy]
PublisherId = ""
DisplayName = "LLM Bikeshed"
Icon = ""

[tool.ruff]
select = ["E", "F", "W", "I"]
target-version = "py310"
